#!/usr/bin/python3

import socket

#f = open("/etc/passwd", 'r')
#sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
#payload = f.read()
#sock.sendto(bytes(payload), ("127.0.0.1", 5555))

# an explanation here f = open("/etc/passwd", 'r')
# some morei sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
# asdfadsf payload = f.read()

# asdfsadf sock.sendto(bytes(payload), (127.0.0.1, 5555))
#uh-oh, don't do thisprint("this is a commented out print statement")
print("this is a valid print statement")
